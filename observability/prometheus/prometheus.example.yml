# Exemplo mínimo de scrape do AiChatFlow. Ajuste host/porta conforme o ambiente.
global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - alerts.yml

scrape_configs:
  - job_name: aichatflow
    metrics_path: /metrics
    static_configs:
      - targets: ['localhost:5001']
