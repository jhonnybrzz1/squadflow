/**
 * Fonte única da squad canônica da mesa redonda (spec 10058).
 *
 * Mantido como re-exportação para compatibilidade; a fonte da verdade agora é
 * `server/orchestration-contracts/roundtable.ts`.
 */

export { DEFAULT_ROUNDTABLE_AGENTS } from '../orchestration-contracts/roundtable';
